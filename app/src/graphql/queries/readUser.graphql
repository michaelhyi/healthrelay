query ReadUser($uuid: String!) {
  readUser(uuid: $uuid) {
    user {
      id
      uuid
      email
      profession
    }
    doctor {
      id
      uuid
      firstName
      lastName
      organization
      phone
      orders {
        id
        date
        priority
        status
      }
    }
  }
}
